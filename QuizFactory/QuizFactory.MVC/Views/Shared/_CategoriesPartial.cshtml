@model IEnumerable<QuizFactory.Models.Category>

<div class="categories">
    <h3 class="green">Categories</h3>
    @foreach (var item in Model)
    {
            <a class="category">
                @Html.DisplayFor(modelItem => item.Name)
            </a>
    }

</div>